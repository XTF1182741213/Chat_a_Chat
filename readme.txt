* UDPService中的Send部分采用多线程，但尚未加同步锁，目前的解决办法是频繁调用时，每调用一次，调用线程等待50毫秒，但单连入的手机较多时可能会有线程安全问题，会导致信息发送错乱。增加等待时间可以减少出错几率，但发送信息的效率降低。根本解决还是加同步锁。
* 有些类，在不同的activity中都有调用，放到java类再导入调用看起来可能会简洁些（做的时候懒。。没有放到工具类，所以就直接粘进去了）
* 其他的，虽然控件对象的命名都是同id，但后面都有注释说明功能，功能模块也配有注释